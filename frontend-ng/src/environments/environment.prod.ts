export const environment = {
  production: true,
  // Relative — frontend is always served from the same origin as the API
  // (single-origin hosting, see backend's UseDefaultFiles/MapFallbackToFile).
  apiBase: '/api',
};
