services:
  postgres:
    image: postgres:16-alpine
    container_name: tehzeeb-pg-dev
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tehzeeb_pos
    ports:
      - "5432:5432"
    volumes:
      - tehzeeb-pg-data:/var/lib/postgresql/data

volumes:
  tehzeeb-pg-data:
